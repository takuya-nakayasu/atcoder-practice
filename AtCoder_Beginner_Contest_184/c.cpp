#include <bits/stdc++.h>
using namespace std;

int main()
{
    int r1, c1, r2, c2, loop_cnt;
    cin >> r1 >> c1;
    cin >> r2 >> c2;

    loop_cnt = 0;

    while (r1 == r2 && c1 == c2)
    {
        int next1, next2;
        r1 + c1 = next1 + next2;
    }

    cout << loop_cnt << endl;
}